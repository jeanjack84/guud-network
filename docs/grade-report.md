# Grade Report — The Guud Network

Auto-generated by the dynamic workflow `workflows/grade.mjs`: a panel of independent judges grading the **live deployment** at https://guud-network.vercel.app against [`rubric.md`](../rubric.md). No human in the loop.

## Final score: 99/100

| Section | Score |
| --- | --- |
| A. Impact | 35/35 |
| B. Demo (35) | 34/35 |
| C. Opus 4.8 Use | 15/15 |
| D. Orchestration (15) | 15/15 |

## Head judge verdict

Final Score: 99/100

Strongest aspect: The product is credible and safe for real women in a domain where that matters most. The matching engine maps a plain-language concern ("painful periods for years, doctor dismisses me") to genuinely on-topic categories and a relevant, non-dismissive specialist, while a working emergency safety net flags a chest-pain query with a real call-911 style note and "not medical advice" disclaimers appear site-wide. This was verified live, not just read in code, and it is the part that separates this from a demo that merely looks the part.

Biggest weakness: Mobile responsiveness and visual polish (B5) were confirmed only via static HTML and Tailwind breakpoints in the code, never via a live multi-viewport render or screenshot. This is the single point docked and the only criterion not backed by direct observed behavior.

Top 2 improvements to raise the score:
1. Close the B5 gap with evidence: capture live mobile and desktop screenshots (or have verify.mjs run a headless multi-viewport render) of the landing page, /discover, and a practitioner profile, and include them in the README. This converts "looks responsive in code" into observed proof and recovers the missing point.
2. Make the persistence path provable end-to-end without internals: B4 was confirmed via code and existing DB-backed reviews because a forged external server-action POST 404'd as expected. Add a thin, documented public endpoint or a verify.mjs step that submits a test recommendation and asserts it appears on a profile after moderation, so reviewers can confirm the full write-to-publish loop externally rather than inferring it.

Minor cleanup worth noting: the Moderation type lists a "negative" tone value that the Zod enum omits (src/lib/ai.ts:110 vs 123). Cosmetic only, no behavioral impact, but tidy it to keep the AI types honest.

## Section detail

### A. Impact — 35/35

- **A1 (10/10)** — Landing page (HTTP 200) clearly communicates both halves of the problem: headline 'Find women's health help you can actually trust.' and subcopy 'Describe what you're going through. We'll match you with practitioners that other women have personally recommended for your specific concern.' README 'Why' section names trust + discovery explicitly: 'where do I find the right help? and who can I trust? Dismissal is endemic and discovery is fragmented.'
- **A2 (10/10)** — POST /api/match with 'painful periods for years, doctor dismisses me' returned genuinely relevant, non-random results. matchedCategories = menstrual-health, endometriosis, hormones-thyroid (precisely on-topic). Top practitioner Dr. Amara Okonkwo is a Gynaecologist & Endometriosis Specialist, bio 'Known for listening first and never dismissing pain', with a topReview directly about debilitating periods. Matching clearly maps concern to the right help.
- **A3 (8/8)** — Tone is safe and non-dismissive (empathy: 'your pain is real and deserves to be taken seriously'). Real safety net confirmed: query 'severe chest pain and trouble breathing right now' returned an explicit emergency safetyNote ('can be a medical emergency. Please call your local emergency number or go to the nearest emergency room right now'). 'Not medical advice' disclaimer present on landing page, practitioner page (dr-amara-okonkwo), and README ('In an emergency, contact your local emergency number'). Data credible: named specialists, cities, verified recommendations, trust scores.
- **A4 (7/7)** — MIT LICENSE file present at repo root (verified text 'MIT License', Copyright (c) 2026 Guud). README documents a clean self-host path: 'Run it yourself' (npm install, set DATABASE_URL, set ANTHROPIC_API_KEY/AI Gateway, npm run db:setup, npm run dev) plus a dedicated 'Self-host for your community' section: 'It's MIT-licensed. Fork it, swap the seed data in src/db/seed.ts for your region's practitioners... deploy.'

### B. Demo (35) — 34/35

- **B1 (8/8)** — curl -o /dev/null -w returned HTTP 200 for both https://guud-network.vercel.app/ and /discover. Both render full HTML; home <title> is 'The Guud Network — Find women's health practitioners you can trust'. A /practitioners/dr-amara-okonkwo page also returned 200 and rendered.
- **B2 (10/10)** — POST /api/match with {"query":"painful periods for years, doctor dismisses me"} returned valid JSON containing: (1) empathetic interpretation ('Living with painful periods for years is exhausting... Your pain is real and deserves to be taken seriously.'); (2) matchedCategories as named topics with emoji (Painful & Irregular Periods 🩸, Endometriosis 🌸); (3) ranked practitioner cards sorted by trust.score (89, 86, 86, 86), each with title, specialties, city, trust object, and a topReview. route.ts unions practitioners across matched categories, dedupes, sorts by trust.score desc, slices to 6. safetyNote field present (null here). Full flow live and working.
- **B3 (7/7)** — Profile page src/app/practitioners/[slug]/page.tsx (force-dynamic) renders: TrustBadge with p.trust.score; a Recommendations section listing DB reviews with author/rating/'Helped with'; and a 'Why women trust her · summarised by Claude Opus 4.8' AI synthesis block. Confirmed in live HTML for dr-amara-okonkwo: rendered trust synthesis paragraph ('Women trust Dr. Okonkwo because she listens and believes them...') plus recommendations. All three required elements present and live.
- **B4 (6/6)** — Persistence is genuine and production-wired: src/db/index.ts uses real Neon Postgres (@neondatabase/serverless) via Drizzle (not in-memory). actions.ts submitReview and recommendPractitioner perform db.insert into reviews/practitioners with AI moderation gating status, then revalidatePath('/'), '/discover', and the profile path. Profile pages are force-dynamic and re-query the DB on each load, so a new approved recommendation appears immediately. My blind forged server-action POST returned 404 (wrong Next-Action id / field-prefix convention, expected when reconstructing the action protocol externally), which does not indicate a broken feature; the code path and live DB-backed reviews confirm persistence works.
- **B5 (3/4)** — On-brand: globals.css loads the actual Guud webfonts (Nib Pro display, Hurme Geometric Sans) sourced from the real guud-theme, with the genuine Guud terracotta palette (--color-plum #893400, blush, sage/mocha, gold). Responsive Tailwind breakpoints present (21 sm:, 4 lg:) and viewport meta is set. UI is cohesive and polished. Docked 1 point: responsiveness/polish verified via static HTML and code only (no live multi-viewport render or screenshot), so I cannot fully confirm flawless mobile layout.

### C. Opus 4.8 Use — 15/15

- **C1 (6/6)** — interpretSymptoms (src/lib/ai.ts:26) uses generateObject with Opus 4.8 (model 'claude-opus-4-8' / 'anthropic/claude-opus-4-8', src/lib/ai.ts:11-13) and a Zod schema producing structured output: empathy (warm/validating sentences), matchedCategories (slugs constrained to the provided list, then validated against real cats at line 60-63), and safetyNote (nullable emergency flag). System prompt is empathy-first and non-dismissive. Live POST /api/match {query:'painful periods for years, doctor dismisses me'} returned genuine empathy ('Your pain is real and deserves to be taken seriously'), mapped correctly to menstrual-health + endometriosis (not random/keyword), and ranked real practitioners. Keyword matching exists only as a fallback. Full credit.
- **C2 (5/5)** — synthesiseTrust (src/lib/ai.ts:87) uses Opus 4.8 generateText with system prompt 'grounded ONLY in the reviews provided... No invented facts, no medical claims', returns null when reviewBodies is empty. Wired into the live practitioner page (src/app/practitioners/[slug]/page.tsx:19) and rendered under label 'summarised by Claude Opus 4.8'. Verified the live summary for dr-amara-okonkwo against seed reviews (src/db/seed.ts:54-57): every claim traces to a real review word-for-word ('seven years...pain was normal', 'ordered the right scans', 'believed me', 'partner, not a patient', 'explained every option without rushing', 'follow-up...thorough', 'first appointment...few weeks but worth it'). No invented facts. Full credit.
- **C3 (4/4)** — moderateReview (src/lib/ai.ts:118) uses Opus 4.8 generateObject returning structured {tone, allow, reason}; system prompt allows constructive reviews and blocks harassment/abuse/doxxing/defamation/dangerous medical misinformation. Wired into BOTH submission paths: submitReview (src/app/actions.ts:47) and recommendPractitioner (actions.ts:104); when allow=false the review is stored as 'pending' rather than 'approved' so unsafe content is not published live. On AI failure it fails safe (holds for review). Minor cosmetic inconsistency: Moderation type lists 'negative' tone but the Zod enum omits it (line 110 vs 123) — does not affect behavior. Full credit.

### D. Orchestration (15) — 15/15

- **D1 (5/5)** — scripts/verify.mjs reads BASE from argv[2]/BASE_URL/localhost (line 9), fetches landing(/), /discover, POSTs /api/match, and loads a /practitioners/<slug> page, then process.exit(failed===0?0:1) (line 96). I ran `node scripts/verify.mjs https://guud-network.vercel.app` live: output '6 passed, 0 failed' and EXIT_CODE=0. Exit code genuinely reflects pass/fail and it asserts the core flow including the Opus match endpoint (6 practitioners matched, empathy string, topics menstrual-health/endometriosis/hormones-thyroid). Wired as `npm run verify`. Full marks.
- **D2 (5/5)** — workflows/grade.mjs is a real dynamic workflow. It parses args as either a bare URL string or {baseUrl, repoPath} (lines 14-18, defaulting to localhost), defines SECTIONS for the 4 rubric sections, runs one independent judge agent per section via parallel() (lines 71-92) against the live URL + repo, then a head-judge agent synthesizes a final /100 verdict (lines 100-110) and returns {baseUrl,total,maxTotal,sections,verdict}. Re-runnable on a new URL via args, as documented in README lines 71-79. This grading run is itself an instance of that workflow. Full marks.
- **D3 (5/5)** — package.json defines `db:setup`: `drizzle-kit push && tsx src/db/seed.ts` — provisions schema then seeds in one command. drizzle.config.ts loads .env.local and points at src/db/schema.ts (postgresql). seed.ts is substantial (16KB): 12 categories, practitioners with reviews, loads dotenv, exits non-zero on error. README 'Run it yourself' documents the clean path (install, provision DATABASE_URL, set ANTHROPIC_API_KEY, `npm run db:setup`, `npm run dev`) plus a 'Self-host for your community' section telling another team to fork, swap seed data, run db:setup, deploy. Minor friction: DATABASE_URL must be provisioned manually before db:setup so it is not literally zero-config one-command, but README documents this clearly and the seed/schema chaining is genuinely repeatable. Full marks.
